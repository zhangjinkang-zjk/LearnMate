<template>
  <div><PageTitle eyebrow="基础讲解" title="建立一套能复用的方法" description="系统根据你的学习目标，把长文档拆成短讲解、例子和即时检查。" /><div class="lesson-grid"><section class="surface surface-pad lesson-main"><div class="lesson-meta"><span>第 2 节 / 共 6 节</span><span>预计 12 分钟</span></div><h2>文档切分为什么会影响回答质量？</h2><p>先从一个真实检索问题开始，观察上下文长度、语义完整度和召回结果之间的关系。</p><div class="lesson-steps"><div v-for="(step, index) in steps" :key="step.title" class="lesson-step"><span>{{ String(index + 1).padStart(2, '0') }}</span><div><strong>{{ step.title }}</strong><p>{{ step.copy }}</p></div></div></div><button class="button button--primary" type="button" @click="completed = !completed">{{ completed ? '已完成本节' : '完成并记录进度' }}</button></section><aside class="surface surface-pad lesson-aside"><p class="eyebrow">本节检查</p><h3>用一句话回答</h3><p class="muted">为什么不能把整篇文档直接作为一个向量？</p><textarea v-model="answer" rows="5" placeholder="写下你的理解…"></textarea><button class="button button--accent" type="button" :disabled="!answer.trim()">提交检查</button></aside></div></div>
</template>
<script setup>import { ref } from 'vue'; import PageTitle from '@/shared/ui/PageTitle.vue'; const answer = ref(''); const completed = ref(false); const steps = [{ title: '提出问题', copy: '整篇文档召回时，相关信息容易被无关内容淹没。' }, { title: '拆解方法', copy: '按照语义边界切分，并为每段保留必要的上下文。' }, { title: '验证结果', copy: '对比切分前后的召回命中率，再决定是否继续调参。' }]</script>
<style scoped>
.lesson-grid { display: grid; grid-template-columns: minmax(0, 1.35fr) minmax(270px, .65fr); gap: 18px; }.lesson-main { display: grid; gap: 18px; }.lesson-meta { display: flex; justify-content: space-between; color: var(--muted); font-size: 11px; }.lesson-main h2 { margin: 12px 0 0; font-size: 25px; }.lesson-main > p { margin: 0; color: var(--muted); font-size: 14px; line-height: 1.75; }.lesson-steps { display: grid; gap: 15px; margin: 10px 0 6px; }.lesson-step { display: flex; gap: 13px; }.lesson-step > span { color: var(--accent-deep); font-size: 12px; font-weight: 800; }.lesson-step strong { font-size: 14px; }.lesson-step p { margin: 5px 0 0; color: var(--muted); font-size: 12px; line-height: 1.6; }.lesson-aside { display: grid; align-content: start; gap: 11px; }.lesson-aside h3 { margin: 0; font-size: 19px; }.lesson-aside textarea { width: 100%; resize: vertical; padding: 10px; border: 1px solid var(--line); border-radius: 5px; outline: none; font-size: 13px; line-height: 1.6; }.lesson-aside textarea:focus { border-color: var(--accent-deep); }.lesson-aside .button:disabled { opacity: .45; cursor: not-allowed; }
@media (max-width: 900px) { .lesson-grid { grid-template-columns: 1fr; } }
</style>
