<template>
  <div><PageTitle eyebrow="学习导航" title="知道自己正在走哪一步" description="导航只负责呈现阶段关系，具体学习仍在任务、讲解和工作区中完成。" /><div class="navigation-list"><div v-for="(item, index) in items" :key="item.title" class="navigation-row surface"><div class="navigation-number">{{ String(index + 1).padStart(2, '0') }}</div><div class="navigation-copy"><h2>{{ item.title }}</h2><p>{{ item.copy }}</p></div><div class="navigation-state" :class="`state-${item.state}`">{{ item.stateLabel }}</div><RouterLink class="text-link" :to="item.to">查看 →</RouterLink></div></div></div>
</template>
<script setup>import PageTitle from '@/shared/ui/PageTitle.vue'; const items = [{ title: '学习概览', copy: '确认目标、掌握度和系统建议。', state: 'done', stateLabel: '已完成', to: '/learning/overview' }, { title: '任务分析', copy: '明确当前目标、学科范围和验收标准。', state: 'done', stateLabel: '已完成', to: '/learning/task-analysis' }, { title: '基础讲解', copy: '补齐概念和通用方法。', state: 'active', stateLabel: '进行中', to: '/learning/fundamentals' }, { title: '进阶学习', copy: '在目标场景中完成迁移和综合实践。', state: 'locked', stateLabel: '待解锁', to: '/learning/advanced' }]</script>
<style scoped>.navigation-list { display: grid; gap: 10px; }.navigation-row { display: grid; grid-template-columns: 54px minmax(0, 1fr) auto auto; align-items: center; gap: 18px; padding: 18px 22px; }.navigation-number { color: var(--accent-deep); font-size: 12px; font-weight: 800; }.navigation-copy h2 { margin: 0 0 5px; font-size: 16px; }.navigation-copy p { margin: 0; color: var(--muted); font-size: 12px; }.navigation-state { font-size: 11px; }.state-done { color: #5a8d61; }.state-active { color: var(--accent-deep); font-weight: 800; }.state-locked { color: var(--muted); }.text-link { color: var(--accent-deep); font-size: 12px; font-weight: 800; text-decoration: none; } @media (max-width: 650px) { .navigation-row { grid-template-columns: 35px minmax(0, 1fr) auto; gap: 12px; }.navigation-state { display: none; } }
</style>
