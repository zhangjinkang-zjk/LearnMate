<template>
  <div><PageTitle eyebrow="学习工作区" title="材料、思考和证据放在一起" description="工作区承接每次学习的输入和产出，支持文档阅读、检索对照、笔记与提交。" slot><template #actions><button class="button button--accent" type="button" @click="saved = !saved">{{ saved ? '已保存' : '保存进度' }}</button></template></PageTitle><div class="workspace-grid"><section class="surface document-panel"><div class="panel-toolbar"><strong>维修手册 · 第 3 章</strong><span class="muted">文档阅读</span></div><article class="document-body"><h2>知识库检索效果下降的常见原因</h2><p>当文档切分边界不完整时，检索结果可能缺少关键上下文。排查时应同时观察召回片段、查询改写和评估指标。</p><blockquote>先找到证据，再做决定。每次调整都要能被复现。</blockquote><p>本案例提供维修手册、检索日志和知识结构图三份材料。请在右侧记录你的判断。</p></article></section><aside class="workspace-side"><section class="surface surface-pad"><p class="eyebrow">你的决策</p><textarea v-model="note" rows="8" placeholder="记录问题判断、依据和下一步…"></textarea><button class="button button--primary" type="button" :disabled="!note.trim()">提交方案</button></section><section class="surface surface-pad evidence"><p class="eyebrow">可用材料</p><button v-for="item in evidence" :key="item" type="button" class="evidence-link">{{ item }} <span>↗</span></button></section></aside></div></div>
</template>
<script setup>import { ref } from 'vue'; import PageTitle from '@/shared/ui/PageTitle.vue'; const note = ref(''); const saved = ref(false); const evidence = ['查看检索日志', '查看知识结构图', '打开评估记录']</script>
<style scoped>.workspace-grid { display: grid; grid-template-columns: minmax(0, 1.3fr) minmax(280px, .7fr); gap: 18px; }.document-panel { min-height: 500px; }.panel-toolbar { display: flex; justify-content: space-between; padding: 17px 22px; border-bottom: 1px solid var(--line); font-size: 12px; }.document-body { max-width: 720px; padding: 40px clamp(22px, 5vw, 62px); }.document-body h2 { margin: 0 0 20px; font-size: 25px; line-height: 1.35; }.document-body p { color: #526057; font-size: 14px; line-height: 1.9; }.document-body blockquote { margin: 24px 0; padding: 15px 18px; border-left: 3px solid var(--accent); background: #f6f8f3; color: var(--accent-deep); font-size: 14px; line-height: 1.7; }.workspace-side { display: grid; align-content: start; gap: 18px; }.workspace-side textarea { width: 100%; resize: vertical; padding: 11px; border: 1px solid var(--line); border-radius: 5px; outline: none; font-size: 13px; line-height: 1.7; }.workspace-side textarea:focus { border-color: var(--accent-deep); }.workspace-side .button { width: 100%; margin-top: 11px; }.evidence { display: grid; gap: 8px; }.evidence-link { display: flex; justify-content: space-between; padding: 9px 0; border: 0; border-bottom: 1px solid var(--line); background: transparent; color: var(--accent-deep); text-align: left; font-size: 12px; }.evidence-link:last-child { border-bottom: 0; } @media (max-width: 900px) { .workspace-grid { grid-template-columns: 1fr; } }
</style>
