# LearnMate 工程协作规范

本文件是仓库级默认规范。修改代码前先阅读，并以当前目录下更具体的 `AGENTS.md` 为准。

## 1. 架构边界

- 前端按 `app / layouts / pages / features / entities / shared` 分层；页面编排放 `pages`，可复用业务能力放 `features`，领域状态放 `entities`，通用 API/UI/工具放 `shared`。
- 后端按 `router -> service -> model` 分层。Router 只做鉴权、参数校验和响应包装；业务规则、事务边界和外部服务调用放 Service；Model 只描述持久化结构。
- 不在页面组件中直接拼接后端 URL、写数据库字段映射或复制业务规则；统一通过 `shared/api` 或后端 Service 访问。
- 新增抽象必须解决真实重复或边界问题，避免为了少量代码创建无意义的通用层。

## 2. 函数与参数

- 函数只承担一个清晰职责；复杂流程拆成有命名的私有函数，避免超过一个屏幕仍无法看懂的函数。
- 参数顺序统一为“核心对象/标识 -> 必填业务参数 -> 可选参数”；可选参数必须有明确默认值。
- 参数超过 3 个时优先使用对象/数据类承载，并在边界处校验类型、长度和取值范围；不要依赖位置参数传递多个相似字符串。
- 函数名使用动词开头，表达实际动作；布尔值使用 `is/has/can/should` 前缀。禁止使用 `data1`、`temp`、`obj` 等无语义命名。
- 不隐式改变入参，不通过全局变量传递业务上下文；异步函数必须明确 `async`，调用方必须 `await`。

## 3. 目录与文件命名

- 目录使用小写 kebab-case 或现有领域名；前端页面目录按业务域命名，组件文件使用 PascalCase，JS 工具/API 文件使用 camelCase。
- Python 模块和目录使用 snake_case；类使用 PascalCase，函数和变量使用 snake_case。
- 同一目录内遵循已有命名风格，不为追求统一而批量重命名无关文件。
- 文件名必须体现职责，例如 `diagnosisApi.js`、`DiagnosisPage.vue`、`diagnosis_router.py`；禁止使用 `new`、`final`、`test2` 等临时后缀。

## 4. API、鉴权与状态

- 前端 API 必须集中封装，页面不得直接创建 Axios 实例。
- 需要用户数据的后端接口必须使用统一 JWT 依赖；前端请求统一携带 `Authorization: Bearer <token>`，兼容旧接口时可同时携带 `token` header。
- 不把 token、密码或敏感信息写入日志、URL、错误提示或提交记录；token 失效统一清理并回到登录流程。
- 服务端返回的用户、学习、诊断状态必须以服务端为准；`localStorage` 只能作为临时缓存或离线草稿，不能替代入库。
- API 响应结构和错误码必须在 Router/Api 层统一解包，页面只处理明确的业务结果。

## 5. 修改与验证

- 修改前先检查 `git status`、相关提交和调用方；不要覆盖其他任务的工作区改动。
- 先复用已有逻辑和提示词，再新增接口或规则；新增提示词必须说明输入、输出 JSON 结构、边界条件和失败兜底。
- 每次改动完成至少运行对应构建、语法检查或测试，并执行 `git diff --check`。
- 提交信息使用 `type(scope): description`，一个提交只表达一个完整目的；不要提交构建产物、密钥、临时文件或无关格式化。
- 完成代码提交后，默认立即推送到当前分支配置的远程跟踪分支，并在交接说明中写明远程分支和提交号；只有用户明确要求“仅保留本地”时才不推送。

## 6. 前端视觉一致性

- 首页已经确定 LearnMate 的主视觉风格；并行智能体制作页面、卡片和组件时，必须以首页现有色板、字体、圆角、边框、阴影、间距和动效为基准，不得另起一套视觉语言。
- 新页面先查找并复用 `frontend/src/shared/styles`、现有布局组件和公共 UI；只有确认无法复用时才新增样式或组件。
- 卡片保持克制、信息优先，避免无必要的渐变、装饰性大色块和层层嵌套卡片；页面结构应与现有 `AppShell`、侧边栏和内容区保持一致。
- 交互状态必须完整覆盖默认、悬停、聚焦、禁用、加载和错误状态，并沿用现有的 `--ink`、`--muted`、`--line`、`--paper`、`--accent`、`--accent-deep` 等设计变量。
- 并行开发提交前应在桌面和移动宽度检查布局，确保新增卡片、文字和按钮不溢出、不遮挡，也不改变已有首页的布局和行为。

## 7. 专精度

- 注意，我们的时间不是很多，尽量每一次都做到最好，你要多想多考虑，而不是只一味的追求完成，当遇到模糊的请求时，你会尽量追问到底而不是不懂装懂。
- 注意，一些后端的现成的代码并不是你调用了就能用，你需要先看看接口相关逻辑在进行判断，之后选择性的直接使用或者微调。切记，你要做的不只是直接用，而是理解运行的逻辑在进行适当修改调用。

## 8. 多分支协作

- 多个智能体可以并行工作，但不得同时修改同一文件或同一模块的公共接口。
- 开始任务时在交接文档或任务消息中声明占用的文件；完成后说明提交号、验证命令和剩余风险。
- 其他分支要获得本规范，优先合并包含本文件的提交或执行 `git cherry-pick <commit>`；不要用强制推送覆盖他人历史。


## 额外

- 后端的环境在conda的zhiban里面